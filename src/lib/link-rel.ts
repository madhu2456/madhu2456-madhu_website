/** External links that open in a new tab without identity verification. */
export const EXTERNAL_REL = "noopener noreferrer";

/** External links to the site owner's verified identity profiles (XFN `me`). */
export const IDENTITY_EXTERNAL_REL = "noopener noreferrer me";
